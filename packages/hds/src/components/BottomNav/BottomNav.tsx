import BottomNavButton from '@/components/BottomNav/BottomNav.button';
import BottomNavRoot from '@/components/BottomNav/BottomNav.root';

const BottomNav = Object.assign(BottomNavRoot, {
  Btn: BottomNavButton,
});

export default BottomNav;
