import { style } from '@vanilla-extract/css';

export const eventBannerCss = style({ margin: '20px 20px 0' });

export const menusCss = style({
  display: 'flex',
  flexDirection: 'column',
  gap: '20px',
  paddingBottom: 200,
});
